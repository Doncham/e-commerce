[mysqld]
server-id=3
log-bin=mysql-bin
binlog_format=ROW

gtid_mode=ON
enforce_gtid_consistency=ON

log_replica_updates=ON
relay_log_recovery=ON

report_host=mysql-replica2
report_port=3306

default_authentication_plugin=mysql_native_password